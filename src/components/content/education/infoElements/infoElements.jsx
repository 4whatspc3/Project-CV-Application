const SchoolInfo = ({ value }) => {
  return <p>{value}</p>;
};

const DegreeInfo = ({ value }) => {
  return <p>{value}</p>;
};

const StartDateInfo = ({ value }) => {
  return <p>{value}</p>;
};

const EndDateInfo = ({ value }) => {
  return <p>{value}</p>;
};

const LocationInfo = ({ value }) => {
  return <p>{value}</p>;
};

export { SchoolInfo, DegreeInfo, StartDateInfo, EndDateInfo, LocationInfo };
